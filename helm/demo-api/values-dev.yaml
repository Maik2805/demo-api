replicaCount: 1

image:
  tag: "1.0.0"

service:
  type: NodePort
  port: 9001

resources:
  limits:
    cpu: 800m
    memory: 600Mi

  requests:
    cpu: 200m
    memory: 256Mi


ingress:
  enabled: true
  className: nginx
  host: demo-api.local
  path: /demo


autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 60